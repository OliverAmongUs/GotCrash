<html>
<head>
<link rel="apple-touch-icon" href="apple-icon.png">
<link rel="shortcut icon" href="favicon.ico">
<link href='https://fonts.googleapis.com/css?family=Open+Sans:400,600,700,800' rel='stylesheet' type='text/css'>

<%= stylesheet_link_tag "admin.css" %>
<%= javascript_include_tag "admin.js" %>
</head>

<body>
    <!-- Left Panel -->
    <%= render "layouts/left_panel" %>
    <!-- Left Panel -->

    <!-- Right Panel -->

    <div id="right-panel" class="right-panel">

        <!-- Header-->
        <%= render "layouts/header" %>
        <!-- Header-->

        <div class="breadcrumbs">
            <div class="col-sm-4">
                <div class="page-header float-left">
                    <div class="page-title">
                        <h1>Bid #<%=@bid.id%></h1>
                    </div>
                </div>
            </div>

            <div class="col-sm-8">
                <div class="page-header float-right">
                    <div class="page-title">
                        <ol class="breadcrumb text-right">
                            <li><a href="/">GotCrash</a></li>
                            <li><a href="/reports">My Reports</a></li>
                            <li class="active">#<%=@bid.id%></li>
                        </ol>
                    </div>
                </div>
            </div>
        </div>


        <div class="content mt-3">
            <div class="animated fadeIn">
            <div class="row">
            <div class="col-lg-10">
            <div class="card">
            <div class="card-body card-block">
              <div class="col-lg-12">
                <h3 class="padding_label">
                  Report id:<span class="grey right"><%= @bid.report_id %></span>
                </h3>
                <hr>
                <h3 class="padding_label">
                  Description:<span class="grey right"><%= @bid.description %></span>
                </h3>
                <hr>
                <h3 class="padding_label">
                  Cost:<span class="grey right">$ <%= @bid.cost %></span>
                </h3>
                <hr>
                <h3 class="padding_label">
                  Did the user ignore it?<span class="grey right"><% if @bid.ignored == 0 %> No <% else %> Yes <% end %></span>
                </h3>
                <hr>
              </div>

              <div class="col-lg-4">
                <%= link_to 'Edit bid', edit_fixer_bid_path(current_user, @bid), class: "btn btn-danger", style: "width:106px" %>
              </div>
              <div class="col-lg-4 col-lg-offset-4">
                <% if current_user.type == 'Owner' %>
                <%= link_to 'Back', report_path(@bid.report_id), class: "btn btn-primary", style: "width:100px"  %>
                <% else %>
                <%= link_to 'Back', fixer_bids_path, class: "btn btn-primary", style: "width:100px; margin-left:160px"  %>
                <% end %>
              </div>
            </div>
            </div>
          </div>
        </div>
      </div>
    </div>



</div><!-- /#right-panel -->

    <script type="text/javascript">
      $(function(){
        $(".navbar-nav>li").eq(1).addClass("active");
      });
    </script>

</html>
